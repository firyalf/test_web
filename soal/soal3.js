/**
 * Membuat Object berdasarkan Array yang di kasih.
 * @dataArray ['ayam', 'bebek', 'angsa']
 *
 * @return sebuah @Object
 */
function ambilInfoArray(dataArray) {
  const dataObject = {
  	var data=[dataArray];
  	//document.write("data");
  	console.log(data);

  	//var panjangData = data.length;
  	//document.write(panjangData);
  }; //membuat variabel Object

  //tempat coding disini

  return dataObject;
}
